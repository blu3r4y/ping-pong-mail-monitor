<html>

    <head>
        <meta charset="UTF-8">
        <title>ÖH Mail Monitor</title>
        <link rel="shortcut icon" href="{{ url_for('static', filename='favicon.ico') }}" type="image/x-icon"/>
        <script src="{{ url_for('static', filename='plotly-latest.min.js') }}"></script>
        <style type="text/css">
            body,
            html {
                margin: 0;
                padding: 0;
                width: 100%;
                height: 100%;
                display: table;
            }

            #chart {
                display: table-cell;
                width: 100%;
            }
        </style>
    </head>

    <body>
        <div id="chart">
            <script>
                var data = {{plot | safe}};
                var config = {
                    responsive: true
                }
                Plotly.newPlot('chart', data, {}, config);
            </script>
        </div>
    </body>

</html>